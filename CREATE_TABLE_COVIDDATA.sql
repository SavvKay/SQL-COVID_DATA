CREATE TABLE PROJECT.COVID_DATA (
	 ISO_CODE VARCHAR(255),
	 CONTINENT VARCHAR(255),
	 LOCATION VARCHAR(255),
	 RECORD_DATE DATE,
	 NEW_CASES INTEGER,
	 NEW_DEATHS INTEGER,
	 REPRODUCTION_RATE REAL,
	 ICU_PTNTS VARCHAR(255),
	 NEW_TESTS VARCHAR(255),
	 POP_DENSITY REAL,
	 MEDIAN_AGE REAL,
	 AGED_65_OLDER REAL,
	 AGED_70_OLDER REAL,
	 EXTRM_POVERTY VARCHAR(255),
	 CARDIOVASC_DEATH_RATE REAL,
	 DIABETES_PREVALENCE REAL,
	 FEMALE_SMOKERS VARCHAR(255),
	 MALE_SMOKERS VARCHAR(255),
	 HANDWASHING_FACILITIES REAL,
	 HOSPITAL_BEDS_PER_THSND REAL,
	 LIFE_EXPECTANCY REAL,
	 HUMAN_DEVELOPMENT_INDEX REAL,
	 PRIMARY KEY(ISO_CODE, RECORD_DATE)
	 -- Because there is no unique identifier in this data, the Primary Key is a Composite Primary Key
	 )